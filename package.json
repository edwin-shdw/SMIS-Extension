{
  "name": "smis",
  "version": "1.0.0",
  "description": "A chrome extension which scrapes images from social media pages",
  "main": "main.js",
  "scripts": {
    "build": "rm -rf extension/JavaScript && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["extension", "scraper"],
  "author": "edwin-shdw",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.242",
    "chrome-types": "^0.1.218",
    "typescript": "^5.1.6"
  }
}
